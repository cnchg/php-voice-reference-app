0.1.0
------------------------------------------------------------

- Basic SIP (server, client) integration

  - application provides an interface to create SIP domains 
  on demand, using your Catapult login
  - application defines a set of rules to listen to incoming/outgoing 
  SIP calls

0.2.0
-----------------------------------------------------------
- add user based callbacks like:
  POST parameters: username=a_username password=a_password
  {server_url}/{users} 
  {server_url}/callback/{username}
  where each user will be stored in the users.json

0.2.1
-----------------------------------------------------------
- load php-bandwidth from vendor/autoload only
composer.lock will be pre-bundled
